<div class="bg-gray-100 items-center justify-center">
  <div class="bg-white shadow-md rounded-lg p-6">
    <h1 class="text-2xl font-bold mb-4 text-center">Jumping Julia Maze</h1>


    <%# <div class="py-2 px-2 max-w-sm mx-auto space-y-2 bg-white rounded-xl shadow-lg sm:py-4 sm:flex sm:items-center sm:space-y-0 sm:gap-x-6">
      <div class="text-center space-y-2 sm:text-left">
        <div class="space-y-0.5">
          <p class="text-lg text-black font-semibold">
            Erin Lindford
          </p>
          <p class="text-slate-500 font-medium">
            Product Engineer
          </p>
        </div>
        <button class="px-4 py-1 text-sm text-purple-600 font-semibold rounded-full border border-purple-200 hover:text-white hover:bg-purple-600 hover:border-transparent focus:outline-none focus:ring-2 focus:ring-purple-600 focus:ring-offset-2">Message</button>
      </div>
    </div> %>
    <div class="container mx-auto px-4 text-center mb-6">
      <%# <div>Your objective is to help Julia get from start to goal: begin on the square in the upper left.</div>
      <div>The number on the square you are standing on tells you how many jumps you must take.</div>
      <div>You can jump horizontally or vertically but not diagonally</div> %>
      <img src="https://jrmf.org/wp-content/uploads/WP-Desktop-jumping-julia2.png" alt="Your objective is to help Julia get from start to goal: begin on the square in the upper left">
    </div>

    <!-- Maze Output -->
    <div class="overflow-x-auto">
      <table class="border-collapse border border-gray-300 mx-auto mb-6">
        <tbody>
          <% @maze.each_with_index do |row, row_idx| %>
            <tr>
              <% row.each_with_index do |cell, col_idx| %>
                <td class="w-16 h-16 text-center font-bold border border-gray-300 
                           <%= row_idx == 0 && col_idx == 0 ? 'bg-yellow-200' : '' %>
                           <%= row_idx == @maze.size - 1 && col_idx == row.size - 1 ? 'bg-red-200' : '' %>">
                  <%= cell %>
                </td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <!-- Form to specify maze size -->
    <form action="/" method="get" class="mb-6 text-center">
      <div class="mb-3 float-left">
        <label for="width" class="text-sm font-medium text-gray-700">Width</label>
        <input type="number" name="width" id="width" min="1" value="<%= params[:width] || 4 %>"
               class="mt-1 w-24 mx-auto px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500">
      </div>
      <div class="mb-3 float-right">
        <label for="height" class="text-sm font-medium text-gray-700">Height</label>
        <input type="number" name="height" id="height" min="1" value="<%= params[:height] || 4 %>"
               class="mt-1 w-24 mx-auto px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500">
      </div>
      <button type="submit"
              class="mt-3 bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">
        Generate New Maze
      </button>
    </form>

  <div class="mb-6 text-center" style="font-size: 9px;">Inspired by <%= link_to "Julia Robinson Mathematics Festival", "https://jrmf.org/" %>. Image above attributed to JRMF website. Made by <%= link_to "Rahul Thathoo", "https://twitter.com/thathoo" %>.</div>
  </div>
</div>
